@model AuMVC.Data.Models.Maintenance
@{
    ViewBag.Title = "DeleteMaintenance";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Maintenance</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(m => m.IssueCode)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.Category)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.DateRaised)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.Item)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.Priority)
        </dt>
        <dd>
            @Html.DisplayFor(m => m.Status)
        </dd>
        <dt>
            @Html.DisplayNameFor(m => m.Note)
        </dt>

    </dl>

    @using (Html.BeginForm("DeleteConfirmMaintenance", "Maintenance", FormMethod.Post))
    {
                @Html.HiddenFor(n => n.Id);

                @Html.AntiForgeryToken()
                <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" />
            |
            @Html.ActionLink("Back to List", "IndexMaintenance")
        </div>
    }
</div>



